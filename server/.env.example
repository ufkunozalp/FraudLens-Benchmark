# FraudLens backend environment template

# Required: API server port.
# Format: integer TCP port.
PORT=3001

# Required: primary MongoDB connection URI (typically Atlas).
# Format: mongodb+srv://user:password@cluster/database?query
MONGODB_URI=mongodb+srv://username:password@cluster.example.mongodb.net/fraudlens?retryWrites=true&w=majority

# Optional: local MongoDB URI used when a user selects local storage mode.
# Format: mongodb://host:port/database
LOCAL_MONGODB_URI=mongodb://127.0.0.1:27017/fraudlens

# Optional: explicit Python interpreter path for exact detector worker.
# Format: absolute path to python3 binary.
EXACT_PYTHON=/usr/bin/python3

# Optional: Hugging Face token (legacy/manual model access; not required in current default path).
# Format: hf_xxx token.
HF_API_TOKEN=hf_your_token_here
